/**
 * Ideiglenes placeholder – a valódi típusokat a `npm run db:types` generálja
 * a Supabase sémából (supabase gen types typescript --db-url ...).
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
